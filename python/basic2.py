# List
server_names = ["mServer", "dServer", "aServer"]

# Dictionary
server_info = {"name": "mServer", "ip": "192.168.0.1", "port": 8000}

for name in server_names:
    print(f"Server Name: {name}")
